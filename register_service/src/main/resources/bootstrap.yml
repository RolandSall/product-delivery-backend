spring:
  application:
    name: register-service

  cloud:
    config:
      uri: http://config-service:8888

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  instance:
    prefer-ip-address: true
    hostname: register-service
